# =====================================================
# SnapLoad UI - Environment Configuration
# =====================================================

# Backend API URL (Client-side - usado por el navegador)
# Development Local: http://localhost:9020
# Production: https://your-api-domain.com
NEXT_PUBLIC_BACKEND_URL=http://localhost:9020

# Backend API URL (Server-side - usado por Next.js server)
# Development Local: http://localhost:9020 (mismo que arriba)
# Docker: http://snapload-api:8000 (red interna Docker)
# Nota: En desarrollo local, debe ser localhost:9020
# En Docker, el docker-compose.yaml lo configura automáticamente
BACKEND_INTERNAL_URL=http://localhost:9020

# Optional: Enable debug mode
# NEXT_PUBLIC_DEBUG=false

# =====================================================
# IMPORTANTE:
# =====================================================
# Si corres en desarrollo local (npm run dev):
#   - Ambas URLs deben ser http://localhost:9020
#   - Asegúrate de que el backend esté corriendo en el puerto 9020
#
# Si corres con Docker (docker-compose):
#   - NEXT_PUBLIC_BACKEND_URL=http://localhost:9020
#   - BACKEND_INTERNAL_URL=http://snapload-api:8000
#   - El docker-compose.yaml ya configura esto automáticamente
